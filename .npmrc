# @vercel/analytics declares optional Svelte peers that conflict with the
# Vite version Angular's builder pins. Without this flag, `npm ci` (CI and
# Vercel builds) fails with ERESOLVE even though the local install works.
legacy-peer-deps=true
